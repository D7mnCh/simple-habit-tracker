# TODO
- set min and max duration for that habit in order to mark it as done
- can be syncronization on both phone and pc
- github style (simple table), it's ok if i didn't impl gradient that depends on min and max 
- no fancy chars, graphs
- choose rust libraries carefuly, i wanna build this app as fast as possible
